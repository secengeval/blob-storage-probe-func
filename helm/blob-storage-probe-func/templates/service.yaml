apiVersion: v1
kind: Service
metadata:
  name: {{ include "blob-storage-probe-func.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "blob-storage-probe-func.name" . }}
    helm.sh/chart: {{ include "blob-storage-probe-func.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "blob-storage-probe-func.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
