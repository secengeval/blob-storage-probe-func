apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blob-storage-probe-func.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "blob-storage-probe-func.name" . }}
    helm.sh/chart: {{ include "blob-storage-probe-func.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "blob-storage-probe-func.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
